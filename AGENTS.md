# 项目实验协作基本规则

本规则来自用户 2026-09-09 的明确要求，适用于后续每次新测试。同时遵守 CLAUDE.md 中的原始数据、历史结果和冻结包保护规则。

1. 尽可能复用项目已有的数据、代码、调度和评价函数；新增实验单独模块化，不把实现堆进 main_new.py。
2. 每次接入新实验时，核对 main_new.py 顶部已有功能开关，保留用户关闭的步骤；每个开关及顶部参数后添加中文注释，准确说明控制的分析或作用。
3. 新实验在 main_new.py 的 STEPS 最后新增一个独立 0/1 开关并加注释，默认关闭。仅开启它即可执行该实验全部授权范围；旧步骤不应被偷偷启动。
4. 每次修改、测试和正式运行都更新 LOG.md，记录本地日期时间、目的、代码/文件路径、具体修改或命令、通过/失败/限制和结果位置。实现与对应日志必须在同一提交中。
5. 所有实验通过 main_new.py 进入，使用 results/new/<YYYYMMDDHHMMSS>/results/<experiment>/ 新目录；记录目的、开关、输入/源码/输出指纹及逐步状态，不覆盖历史结果。
6. 是否执行正式实验遵循当轮用户要求：仅要求离线测试时不得替用户运行；明确授权完整执行时连续完成，不重复请求继续。运行可临时在调用中开启当前实验，交付文件的开关默认保持关闭。
7. 执行前冻结该轮协议和比较口径；必要数值修正记录原因及重算范围，不根据候选排名修改设计，不隐藏失败、稀疏样本或阴性结果。
8. 按普通结果目录及 Markdown 报告交付，供人工与 agent 联合审查。只有用户明确要求时才创建回传压缩包或同步 GitHub；不能因旧任务曾发布而自动发布新实验。

DD-AGG01 当前授权：完整执行指令 docs/new_analysis/instructions/Codex_DD_AGG01_日度阵风聚合单因素测试.md；用户本轮补充的“不生成回传包”覆盖文件第 12 节的 zip 要求。禁止天气请求、新协变量/模型形式/时间划分、Word 修改和历史覆盖。用户已决定保留 District-day 正文章节，不能沿用旧实验自动删章建议。

## 2026-09-10：附录生产接口的明确例外

用户另行授权根目录 `main_appendix.py` 作为 A–J 附录生产入口。这是既有结果的整理接口，不是新的实验；不接入或启动 `main_new.py` 的研究开关。按本轮明确指令，产物使用固定 `results/Appendix/`，无时间戳目录；只更新选中字母，暂存校验后替换，清理仅限该接口清单管理的废弃文件，保留未知人工文件。`--check-only` 不写任何产物或日志。正式生产及开发/测试仍记入 `LOG.md`。原研究入口的默认行为与时间目录规则保持不变。

设计依据为 `docs/new_analysis/instructions/论文整合计划_v9_附录成文与正文衔接.md`；本轮只授权映射、独立导出、实际运行与必要核验，不授权完整附录写作、正文修改、重训、关闭事项重审、回传包或远程发布。运行与维护说明见 `docs/APPENDIX_PRODUCTION_GUIDE.md`。

## APP-C-COMPLETE 范围内的新授权

用户明确授权仅Appendix C在最终四组样本上补拟合既定候选及原设计CV，取代上一段对C的不得重训限制。通过main_appendix.py选择C独立运行，保留固定目录和事务替换。其他附录、原研究运行和正文不修改，不启动其他缺口或D的bootstrap/profile审计。逐组件复用，缺失计算前冻结配置；新增预测与训练折预处理保存于C/data，完成记录于C/logs与LOG.md。


## APP-J03-COMPARE 第一工作包授权

用户按 docs/new_analysis/instructions/附录FGHIJ四步推进台账与第一步J03执行指令.md 授权仅J03的两来源八任务稳定拟合、固定LAD-CV和固定OOF整LAD配对区间。通过 `main_appendix.py --appendices J --j03-only`，固定写入results/Appendix/J，先暂存核验再替换，其他J需求及其他附录产物保留。每步保存结果汇总与执行回传Markdown，不生成ZIP、不发送远程。执行完成标为待研究负责人反馈分析，不自动科学关闭；F02、G/I、H03须待前包讨论后另行设计。不得重开旧支持上限/g50、跨层级结点或锚点诊断。

## APP-F02-COV 第二工作包授权

研究负责人已确认J03匹配比较缺口关闭、保留正文PROXY；只更新既有台账及根状态，不重跑或改写J。当前授权F02两个天气固定final模型的LAD-only与LAD/date协方差、SE及区间对照。优先复用兼容X/残差/系数；必要时每模型固定OLS一次，不进行CV、结点搜索、bootstrap或F.3计算。入口 `main_appendix.py --appendices F --f02-only`，固定F目录、事务替换，仅管理F02前缀产物。逐步LOG与事实报告照常，完成后待反馈；不启动G/I、H、科学独立审查或远程发布。

## APP-GI-PRED 第三工作包授权
研究负责人按更新台账(2)第10–11节确认J03/F02结果补全关闭，仅授权G/I底层预测、控制残差、原固定模型五折缺失逐行输出与七场既定风暴结果。入口main_appendix.py，固定G/I目录及事务替换；允许必要相同固定OLS，不运行C大比较、D审计或H。状态产物完成/待反馈，逐改动运行记录LOG，其他附录与历史源只读。

## APP-H03-LINK 第四工作包授权

研究负责人按更新台账(3)第12–13节确认前三包结果补全关闭，保留GI-W01至W09写作待改。仅授权H当前正式全体样本的原NB2/Tweedie暴露、Gamma/Tweedie恢复固定X有限拟合及正确score/Hessian两维聚类推断，OLS复用；H2/H3只整理已有结果与必要原分箱频率。main_appendix.py选择H/H03，固定目录事务替换，其他附录/正文只读；不CV、bootstrap、搜索、新模型或成文。结果完成待反馈，更新LOG，停止于事实报告。
