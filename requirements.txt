# pyOutageGust — third-party dependencies for the live code under
# scripts/, paper_revision_work_v2/{R02,R05,X02_storm_specialization,
# R02_isolation_audit,code}, review_package/code/, src/, test/, docs/.
#
# Versions pinned to match the ones already verified working together in
# D:\Pyprogramme\STST2603\requirements.txt (Python 3.12.6), for consistency
# with the project this codebase was migrated from. See
# docs/migration_history/external_dependency_audit_20260908.md for how this
# list was derived.
#
# NOT included here: paper_revision_work_v2/R03 and R04 -- each ships its
# own fully independent vendored runtime under
# paper_revision_work_v2/{R03,R04}/runtime/site-packages/, and does not use
# this environment at all.
numpy==2.4.3
pandas==3.0.1
scipy==1.17.1
matplotlib==3.10.8
pillow==12.1.1
statsmodels==0.14.6
patsy==1.0.2
scikit-learn==1.8.0
geopandas==1.1.3
shapely==2.1.2
openpyxl==3.1.5
pyarrow==25.0.1
joblib==1.5.3
tqdm==4.67.3
SALib==1.5.2
dcor==0.7
distfit==2.0.2
openmeteo_requests==1.7.5
requests-cache==1.3.1
retry-requests==2.0.0
# Not present in the old STST2603 root requirements.txt -- this is the one
# gap identified by the dependency audit (used only by
# scripts/test1/test1_full_pipeline.py). No previously-verified version to
# match, so left unpinned; pin it once installed (see environment.yml for
# the resolved version actually in use).
diptest
