# --- large / read-only external inputs, not tracked in git ---
# Frozen copies of raw data from the old STST2603 project. See
# docs/migration_history/ and data/external/README.md for provenance and
# how to regenerate them.
data/external/

# Generated by scripts/model_review_package_20260907/materialize_final_data.py;
# reproducible from scripts + data/external, not source of truth.
review_package/data/

# --- vendored / snapshot environments (large, not our code) ---
# R03 and R04 each ship their own fully independent Python runtime + set of
# installed packages, snapshotted at a point in time. Not tracked.
paper_revision_work_v2/R03/runtime/site-packages/
paper_revision_work_v2/R04/runtime/site-packages/

# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.ipynb_checkpoints/

# --- conda / venv ---
.venv/
env/
venv/

# --- reviewer feedback (professor's comments), handled outside git ---
Comments/

# --- oversized redundant archive (exceeds GitHub's 100MB blob limit; the
# same 696 files are already tracked unpacked under
# paper_revision_work_v2/X02_storm_specialization/runs/X02_20260907_001/,
# this .zip is just a bundled duplicate of that) ---
paper_revision_work_v2/X02_storm_specialization/RETURN_PACKAGE_X02.zip

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.idea/
.vscode/
