# 附录 F–J 四步推进台账与第一步 J03 执行指令

日期：2026-09-10。

## 1. 当前决定

Appendix A–E 的结果已由研究负责人确认补全，其文字由研究负责人自行审核。本轮不检查 A–E 写作，不修改正文。

F–J 按四个工作包依次推进：**J → F → G＋I → H**。

每包采用同一循环：**设计 → 发送本地执行 → 反馈分析 → 关闭 → 下一步设计**。不因代码执行成功直接认定科学结论成立，也不要求结果必须有利才关闭任务。

只有当前包收到反馈并完成讨论后，才细化下一包的执行设计。不自动连续运行后续工作包。

## 2. 四步范围和关闭依据

| 顺序与编号 | 范围 | 核心交付 | 关闭依据 | 当前状态 |
|---|---|---|---|---|
| 1 / APP-J03-COMPARE | 正文 proxy 与独立天气日最大值，采用统一稳定拟合方法作八任务比较 | 匹配样本与折、最终参数、OOF评分、配对差异及既定区间、校准表图 | 已形成完整且可解释的对照；失败/不确定结果如实呈现，无未解释的实质计算阻塞 | 已关闭结果补全；2026-09-10反馈依据见第8节 |
| 2 / APP-F02-COV | 天气暴露与天气恢复最终模型的 LAD-only 与 two-way SE | 同样本同设计矩阵的系数/协方差对照 | 两类SE可逐项对应，源模型及计算规则明确 | 已关闭结果补全；反馈依据见第10节 |
| 3 / APP-GI-PRED | G 的实际图形底层数据与 I 的最终模型风暴表现 | 区分控制模型残差、最终拟合预测、OOF；图表源数据及七场风暴汇总 | G 可追溯到数据，I 对应当前模型并注明预测性质 | 已设计待发送，见第11节 |
| 4 / APP-H03-LINK | 当前最终样本上既定替代分布模型 | 明确响应、样本、link和比较尺度的有限候选结果 | 所保留的替代模型主张具有对应结果；不适用或失败候选明确处理 | 待第三步关闭后设计 |

### 状态使用规则

- 待设计：只有范围，没有冻结执行细节。
- 待发送：设计与命令已写好，尚无本地执行证据。
- 执行中：由用户告知命令已发送或收到运行记录后登记。
- 待反馈分析：收到结果/人工审核材料后进入。
- 已关闭：结果已完成讨论，实质缺口解决或明确接受其边界。
- 需局部修复：只记录当前包中的具体阻塞，不扩展为其他包。

当前J03、F02均已关闭结果补全；第三步G/I已设计待发送；第四步H尚未启动。第4–6节、第9节保留历史设计，不是再次执行指令。

## 3. 持续记录与目录规则

本地继续以 `main_appendix.py` 为入口；结果写入固定 `results/Appendix/<字母>/`，并在 `figures/`、`tables/`、`data/`、`logs/` 分类。

- 稳定文件名，新输出覆盖旧输出，不生成时间版本目录。
- 每包只更新对应附录和必要的根映射/状态；其他附录和历史研究输出保持原样。
- 表图数据、参数、配置、来源指纹及执行命令保留在本地。记录时间可以作为元数据，不形成目录版本。
- 人工＋agent 的科学审查由研究负责人安排；执行命令不自动启动独立审核或下一包。
- 不生成ZIP或回传包，不自动发布、发送导师或远程提交。
- 用户已关闭的旧支持上限/g50、跨层级结点联系、锚点诊断等不自动重开。
- **第一步明确重新授权 J03 必需的 proxy 拟合与匹配比较**，不受此前“仅导出、不拟合”限制。该授权不扩展到无关历史审计。

本地可在既有任务记录中维护本表；不要另建与现有 catalog 或台账冲突的权威数据源。

## 4. 第一步：APP-J03-COMPARE 的研究设计

### 4.1 要回答的问题

在相同地区日、结局定义、模型形式和LAD分组评价下，使用正文事件锚点 proxy 或独立天气日最大值，会使概率预测表现发生多大变化？

这是两套**天气暴露构造方案的整体比较**。它不单独识别位置、时间聚合和天气获取方式各自的影响，不把任何一方当作观测真值。

### 4.2 两个方案

| 标识 | 定义 | 来源定位 |
|---|---|---|
| PROXY | 正文采用的同日事件锚点插值阵风，保留当前规则 | 当前正文生产面板；先前定位为运行 `20260909183317` 的 `results/final_models/district_day_panel.csv`，以本地已确认登记为准 |
| GRID_MAX | 已下载独立天气逐小时阵风的日最大值 | DD-AGG01 `20260909231634` 中修复后的 A01 数据和结果 |

原始小时天气不重新请求，proxy不改带宽、不换锚点、不重新设计。单位及日期口径读取已接受来源。两方案不互相改名：PROXY不称为日最大值。

### 4.3 样本、结局与评价划分

- 研究期：2021-04-01—2024-03-31；预期111 LAD、1096日、121656地区日。
- 使用同一 LAD-day 键集合、同一八标签和相同观察权重。
- 八任务：全体和天气归因各自的任意事件、>5、>100、>1000客户。
- 标签是至少一个符合条件的事件，不是当天客户数相加；任意事件保留原有零客户记录。
- 主要任务：全体 >100；重要次级：天气归因 >100；其余六项完整展示。
- 复用 DD-AGG01 的实际五折LAD分配；不重新随机分折、不增加时间验证。

先核对两来源是否符合上述定义。出现键或标签差异时，定位数据来源即可；不通过静默取交集、填零或改标签掩盖差异。无法解决的样本不一致属于本包具体阻塞。

### 4.4 模型与拟合

两方案均采用同一个背景率lognormal模型：

\[
p(g)=p_0+(1-p_0)\Phi\!\left(\frac{\ln g-\ln\theta}{\beta}\right).
\]

复用 DD-AGG01 已接受的稳定拟合实现及冻结配置，包括稳定似然计算、参数化、边界、多初值和终止/备用规则。不能只要求都返回 `success=true`。

每方案每任务有一个全样本拟合和五个训练折拟合：**2×8×6＝96个目标拟合组件**。这是应交付的覆盖数量，不等于必须进行96次新拟合。

兼容的修复后 GRID_MAX 组件可复用；PROXY 已有完全兼容组件也可复用。复用条件是样本、模型、实际拟合目标与配置、折号和必要有效性记录匹配。不同运行时间本身不构成不可比。复用与新拟合都写入同一份对照清单。

折内参数只能由训练数据估计；全样本拟合用于附录参数说明，不能生成OOF评分。两套天气的参数分别估计，不固定为相同数值。

新生成的proxy结果不直接覆盖正文历史模型产物。如与正文现有参数/曲线产生实质差别，记录差别及影响，留给反馈分析处理。

### 4.5 评分、配对差异与校准

统一采用逐行池化 OOF Brier：

\[
BS=\frac1N\sum_i(p_i-y_i)^2,\qquad
\Delta BS=BS_{\rm PROXY}-BS_{\rm GRID\_MAX}.
\]

正差表示GRID_MAX更低误差，负差表示PROXY更低误差。相对改善为 `100×ΔBS/BS_PROXY`，不是概率百分点。

每任务同时给出：

- 两方案 Brier、上述绝对/相对差异；
- 同一训练折发生率常数基准及两方案BSS；
- 平均预测、实际发生率和偏差；
- 逐折差异及实际折样本量；
- 配对95%条件区间。

配对区间沿用已采用的固定OOF、整LAD有放回抽样方法，1000次、两方案使用同一抽样。每次按抽中LAD携带的全部地区日重算池化差异；不对五折指标简单做独立样本检验。区间不包含重新拟合、选择和跨LAD共享风暴日期的全部不确定性。不得将新点估计与旧P03区间拼接。

校准优先复用项目已有不依赖结局选择切点的共同分箱规则，给两方案相同边界。配置在比较前记录；如果现有函数不适用，再确定一个简单固定规则并记录，不能看标签后反复调箱。输出各箱n、阳性数、预测与观察频率；稀疏箱与空箱如实标识，不删不利箱。

不要求新增校准置信区间、不设置新的5%通过门槛、不用跨任务平均分数掩盖主要任务或失效拟合。未估计成功的任务明确标记，不用旧失效结果填满表格。

### 4.6 结果在论文中的用途

这次产物支撑 J.2 的两来源比较，独立于 J.3–J.5 已完成结果。两种方案的绝对风速和参数含义可能不同，不把参数大小直接解释为网络更脆弱。

PROXY更好、GRID_MAX更好或差异混合均为可接受的实验结果。项目保留正文主线的决定不预设本次胜负；如新拟合实质改变正文已有数字，结果说明中准确列出，反馈讨论后再决定文字处理。

### 4.7 最小交付

沿用既有命名规范，至少具备：

- 当前协议、来源/组件复用清单、键/标签/折匹配记录；
- 16组全样本参数及80个训练折拟合的状态/参数记录；
- 两来源八任务的OOF预测或可复现的明确来源；
- 八任务匹配评价主表、逐折指标、配对区间、共同分箱表；
- 两个主要任务的比较/校准图，其余任务完整数值可查；
- 必要技术检查和执行说明；
- J03状态与根需求—产物映射更新。

完整结果在本地保存，不要求以压缩包回传。执行说明记录计算事实，不自动代替人工科学审查。

## 5. 第一步可直接发送的 Codex 指令

```text
任务：APP-J03-COMPARE——补齐正文proxy与独立天气日最大值的八任务对照。

请阅读本文件第1–4节及项目AGENTS.md、main_appendix.py、附录登记和J当前结果。
本轮只执行第一工作包；F、G/I、H保持待设计状态。

【授权】
研究负责人明确允许本包所需的proxy及网格模型拟合、固定LAD-CV和已有方法的配对区间计算。
此前“仅整理不拟合”的限制在此范围内被替代，不为这些既定操作再次请求确认。
不开展关闭的旧支持范围/g50、跨层级结点或锚点剔除审计，不重新运行DD-TIME01、
DD-DUR01、DD-AGG01的全部候选，也不修改正文。

【接入】
复用现有main_appendix.py，在J下增加可单独调用的J03子任务或等价配置。
准确调用方式写入README。普通导出可复用有效缓存，另提供明确的本包重算方式。
不另建main入口，不改变旧main_new.py的时间机制。
所有代码无需agent/LLM运行。

【先冻结输入与协议】
1. 按当前登记定位正文PROXY面板，及DD-AGG01修复后A01 GRID_MAX来源。
2. 核对111 LAD、研究期、LAD-day键、八标签、权重及同一五折LAD分配。
3. 不能静默取交集或改变标签/日期口径。只定位这次比较所需来源，不做全库审计。
4. 冻结协议、拟合配置、评分符号、配对抽样种子与共同校准分箱规则。
5. 明确主任务全体>100，重要次级天气>100，其余六任务完整保留。

【拟合与复用】
6. 两方案复用DD-AGG01已经接受的稳定lognormal背景率拟合器和配置，
   不修改模型形式、不增加新初值搜索策略或调优预算。
7. 覆盖2来源×8任务×(全样本+5训练折)=96个目标组件。
   兼容且有必要有效性记录的结果直接复用；不兼容或缺失的完成拟合。
   按样本、目标函数/配置、折号判断兼容，不要求必须来自同一个旧运行。
8. 训练折参数用于OOF预测，全样本参数另存；不得混用。
9. 数值检查沿用稳定拟合器已有规则。必要重试只按冻结策略执行。
   仍无效则准确记录，不以success标志或旧失效解冒充有效结果。
10. 新结果写入J，原正文生产参数与历史结果保持原样。
    如新proxy参数/预测与已存正文结果有实质差别，保存对照记录，
    不因此自动修改正文或启动扩大审计。

【评分与图表】
11. 使用第4.5节定义的Brier、BSS、ΔBS、相对差异、总体偏差及逐折指标。
12. 以固定OOF整LAD配对抽样1000次生成新的条件95%区间。
    两来源共用抽样，按地区日权重重算，不拼接旧区间，不增加重训bootstrap。
13. 两方案使用同一套校准箱，保留样本和阳性计数，空箱留空、稀疏箱标注。
    不按结果反复改箱，不以分箱RMSE替代Brier。
14. 生成八任务完整对照表，主要和重要次级的必要比较图。
    其他任务全部结果可查，不能按有利方向筛选任务。

【目录与更新】
固定输出results/Appendix/J/的figures、tables、data、logs。
使用稳定语义文件名，例如J03_PAIRED_METRICS、J03_CALIBRATION等；
沿用统一图表登记，避免与J其他实验文件冲突。
新输出覆盖本子任务管理的旧输出，不生成时间戳目录。
先暂存与验证再替换；只清理本子任务已经管理且失效的文件。
不能删除J.3–J.5已有产物、人工文件或其他附录结果。
时间可以记录为元数据，不成为文件版本。
更新J README、manifest、根映射和result_gaps中的产物状态。
本地执行完成可标“产物已补齐/待研究负责人反馈分析”，
不自动将总台账中的科学讨论状态写成“已关闭”。

【必要检查】
键/标签/折一致；无训练测试混用；概率及拟合状态有效；
评分可从保存预测复算；配对抽样确实相同；复用配置匹配；
输出覆盖正确且其他附录和J已有实验结果不变。
仅做这些与本任务有关的检查，不重跑项目全部历史测试。

【结束】
实际完成可执行计算及结果输出，不只交脚本骨架。
输出简短执行说明，记载实际复用/新拟合数量、评分表、异常和待人工事项。
如局部任务阻塞，完成其余任务并明确哪些比较不可用。
不自动撰写独立科学审核报告，不修改论文，不生成ZIP，不远程发布。
到结果交付处停止，后续由研究负责人安排人工＋agent审查并反馈。
不得自动进入F、G/I或H。
```

## 6. 收到第一步反馈后的分析框架

本地由研究负责人完成审查后，讨论：

1. 输入、拟合与评价是否完成，是否有真实的结果阻塞？
2. 主要及次级任务的差异方向、幅度与条件区间如何？
3. 其余任务和校准结果是否支持同一解释，还是呈现差异？
4. 新proxy拟合是否实质影响正文已有数字？只处理有证据的具体影响。
5. J03对照能够支撑什么附录表述，是否可以关闭？

无明确优势、差异混合或某候选确实不适用，都不自动构成继续试验的理由。关键是结果完整可解释，不能用未解决的数值故障冒充科学结论。

关闭后才细化F02。后续各包照同样循环推进。

## 7. 反馈与关闭记录

| 工作包 | 本地执行/审查文件 | 关键发现 | 实质阻塞与处置 | 关闭依据及用户决定 | 下一步 |
|---|---|---|---|---|---|
| APP-J03-COMPARE | J03_EXECUTION_REPORT.md；APP_J03_COMPLETION.md；README(2).md | 96/96有效；重点任务差异小；新旧proxy实质一致 | 无报告内未解决计算阻塞；未直接检查本地校准数值 | 本轮反馈判定关闭结果补全，保留解释边界；非独立全代码审计 | F02设计已完成 |
| APP-F02-COV | F02_EXECUTION_REPORT.md | 原表已有se_lad；0新增OLS；SE差约10^-13；补齐矩阵/区间 | 无单系数对照阻塞；暴露非PSD记录及实际曲线用途边界见第10节 | 本轮反馈判定关闭结果补全 | G/I设计见第11节 |
| APP-GI-PRED | 已设计，待用户发送 | 图形身份→逐行证据→G/I输出 | 尚无执行结果 | 未关闭 | 关闭后设计H03 |
| APP-H03-LINK | 未开始 | — | — | 未关闭 | 再讨论F–J成文 |

本文件保留初始设计并持续登记实际反馈。J03的关闭依据与F02指令分别见第8、9节；未在本对话执行本地项目计算。


## 8. J03反馈分析与关闭记录（2026-09-10）

### 8.1 本次可核实的证据范围

已阅读全文：J03_EXECUTION_REPORT.md、APP_J03_COMPLETION.md、README(2).md，并对照第4–6节既定协议。README内J03报告与单独执行报告一致，是同一执行证据的重复呈现，不是另一次独立验证。

本对话复算了八行Brier差、相对差及BSS反推的共同基准；报告精度内一致。未收到本地原始预测、协方差、图像或校准CSV，因此不声称独立重跑或亲自完成图像数值核验。此证据范围不构成追加整包审计或重新拟合的理由。

### 8.2 完成情况

| 验收内容 | 回传结果 | 判断 |
|---|---|---|
| 同地区日、标签和五折 | 111 LAD×1096日=121656；八标签逐行一致；复用DD-AGG01折 | 满足报告层面的输入匹配要求 |
| 统一稳定拟合 | 48个PROXY补算，48个GRID_MAX兼容复用；96/96有效 | 原缺失对照已补齐 |
| 全样本与OOF分离 | 16全样本、80训练折；OOF用训练参数 | 满足预定评价设计 |
| 评分与条件区间 | 保存完整八任务、逐折及1000次整LAD固定OOF配对区间 | 不再拼接旧点估计或旧区间 |
| 校准产物 | 报告列明共同分箱表、两个重点图及稀疏箱支持 | 产物已交付；当前不判校准胜负 |
| 新旧正文proxy | 参数及同输入概率近乎一致 | 未显示正文核心数字需实质修改 |
| 工程集成 | 独立入口、缓存、固定输出、其他结果保护均已记录 | 无尚未解决的运行阻塞 |

两次失败发生在图形后端和评分列名映射，随后成功发布；按完成说明，未改变拟合预算或混用失败暂存结果。跨尝试144次PROXY组件执行不等于最终存在144个独立模型；最终仍是48个唯一PROXY组件。该运行经历本身不构成科学缺陷。

“弱识别标记0”仅表示未触发本地既定诊断，不能扩写为所有参数已被数据充分识别或所有高风速曲线受到充分支持。

### 8.3 结果解读

差值沿用ΔBS=BS_PROXY−BS_GRID_MAX；负值偏向PROXY。

| 任务 | PROXY BS | GRID_MAX BS | ΔBS | 95%配对条件区间 |
|---|---:|---:|---:|---|
| 全体>100，主要 | 0.0559632708 | 0.0560104363 | −0.0000471655 | [−0.0001382098, 0.0000441996] |
| 天气>100，重要次级 | 0.0189333388 | 0.0189854173 | −0.0000520785 | [−0.0001369352, 0.0000339717] |
| 全体>1000 | 0.0081984701 | 0.0082334901 | −0.0000350200 | [−0.0000577333, −0.0000114565] |
| 天气>1000 | 0.0024452928 | 0.0024748567 | −0.0000295639 | [−0.0000490967, −0.0000105613] |

两重点任务GRID_MAX的BS相对PROXY分别高约0.0843%、0.2751%，条件区间均跨0。其余六项中，全体任意事件和>5略偏向GRID_MAX，四个天气归因任务均偏向PROXY；总计6/8偏向PROXY。八项不是互相独立的重复试验，不能把6/8作为额外统计检验。

两个>1000任务GRID_MAX的BS相对PROXY高约0.4272%、1.2090%，条件区间均位于0以下。应保留这一方向性结果，不能笼统写“所有区间跨0”或“两种方案完全没有差别”。同时这只是固定OOF、整LAD重抽样下的逐任务条件证据，没有包含重新拟合或共同风暴日期的全部不确定性；不升级为普遍的气象精度优势。

两来源八项BSS均为正：两者在本次LAD留出中均优于共同训练折发生率基准。较低BS说明该评价下概率误差较小，不单独证明校准更好、物理风速更准确或事件依赖已经消除。

新旧PROXY全样本最大绝对概率差为5.2632114×10^-6，即0.0005263个概率百分点；主要和重要次级分别约9.90×10^-7及3.58×10^-7。最大NLL改善约10^-7，未发现本次稳定重拟合实质改变正文proxy曲线的证据。论文通常报告精度下不需要因此改动主结果；正式生成附录时使用本轮完整精度来源。

### 8.4 论文组织与证据用途

- J.2使用本轮完整八项对照及配对条件区间，替代含历史无效解的比较。不是新增正文研究主线。
- J.3–J.5既有聚合、持续性及时间结果保持原样。
- 正文§3.5/§4.4后续增加J.2的简短指向；§5.5将“独立网格天气尚待替换”的将来式改为已进行替代比较的事实。
- 合适结论：在本次匹配的空间留出比较中，独立日最大天气未显示主要任务的预测优势；正文proxy维持竞争力，稀有阈值结果偏向proxy。
- 保留限制：事件依赖的天气构造尚未被本实验排除，两个来源的整体差异不能归因于单一空间或聚合因素。
- 当前不修改Word或附录正文；写作阶段再核对已存在的校准表图后描述校准，不因本次回传未附图而追加计算。
- 完成说明将审计/组件文件也登记为Table J20–J29。这些是生产登记，不要求将所有过程表都排入论文；成文时核心为八任务评分、必要参数和校准，组件清单继续留作复现记录。

**处置：J03结果补全关闭，进入F02设计。此处关闭的是完整、有效、可解释对照的缺口，不是宣布proxy全部局限已解决。**

## 9. 第二步F02设计与本地Codex指令

### 9.1 要回答的问题与范围

对同一个天气归因最终回归，考虑同一日期跨地区的相关性后，标准误及系数区间相较仅按LAD聚类改变多少？

这是附录F.2的推断口径补全，不是再次筛选模型。只处理天气暴露、天气恢复两个已接受最终模型。F.1完整系数可引用结果；F.3增量R²不重算，G/I与H不启动。

按已接受C报告，天气暴露样本9857、天气恢复9254；恢复保留正式正客户清洗。天气暴露final为11/24平台并含温度平方；天气恢复final为固定11单结点并含温度平方、gust–precip交互。完整设计矩阵以本地最终生产函数为准。**不能把天气恢复误换成全体恢复二次型，不能把C中训练折重选结点的候选当成正式固定结点模型。**

### 9.2 最小计算和验收

优先复用拟合对象，或同样本X、残差及聚类键，计算LAD-only与LAD/date two-way协方差；若这些缺失，允许恢复相同X、y并各拟合一次固定OLS。不需要任何CV、bootstrap、结点搜索或候选比较。

two-way指同时按地区和日期两个维度聚类，不能只将LAD-date联合编码后做一次聚类。沿用已接受项目的小样本修正、参考分布与自由度规则并明示。作为实现参考，[statsmodels两组聚类协方差文档](https://www.statsmodels.org/stable/generated/statsmodels.stats.sandwich_covariance.cov_cluster_2groups.html)说明接口使用模型设计矩阵和残差，并返回联合及单组协方差；实际参数行为以本地安装版本和现有正确实现为准，不为此升级依赖。

关闭依据是两模型两套SE可逐系数对应、同系数同样本、来源规则清楚、能与已存two-way结果解释性对齐；不要求SE一定变大，也不要求某系数仍显著。

### 9.3 可直接发送的指令

```text
任务：APP-F02-COV——补全天气最终模型LAD-only与two-way标准误对照。

【承接状态】
J03已经完成反馈分析，关闭的是匹配比较的结果缺口；保留正文PROXY主线。
请在既有四步台账中记录本文件第8节的结论和来源，不重跑J03。
本轮只执行第二包F02。A–E成果已完成，文字由研究负责人审核；G/I、H尚未启动。

【读取与定位】
阅读AGENTS.md、现有main_appendix.py和附录F登记、当前最终weather_only模型函数、
完整two-way表、已接受C的样本及final规格说明。
只定位本任务必需材料，不做全库版本审计。
正文基线是Extended_paper_draft.docx的已接受本地生产版本。
明确天气暴露、天气恢复的真实最终模型，不以时间最新或候选最优自动替换。
预期样本E_weather=9857、R_weather=9254，实际样本由正式管线决定。
天气恢复是正式固定11单结点final，不是全体恢复二次型，也不是C的折内重选候选。

【冻结与最大限度复用】
1. 记录每模型样本ID、响应变换、完整设计列/列序、标准化参数、交互项、
   固定结点、参考水平、LAD键和日期键定义，以及来源文件/函数/指纹。
2. 复用兼容的最终拟合对象，或X、残差及聚类键；系数表本身不足以计算新SE。
3. 如必要对象缺失，授权按已接受的相同设计各重建/拟合一次OLS。
   不重新选择结点、原因类别、控制变量或样本，不为凑样本数丢行。
4. 复用项目已核实的协方差函数。计算LAD-only及LAD/date two-way；
   two-way不得用只按LAD-date交叉组聚类替代。
   两种协方差必须使用同一X、同一残差和同一系数。
5. 保存小样本修正、各维聚类数量、交叉组数量、设计秩、区间参考分布与自由度。
   沿用正式推断规则，不依赖未记录的软件默认值，不为了对齐数字改规则。
6. 核对新two-way与已有final two-way表。同一模型的实质差异才定位，
   先检查矩阵、样本、标准化、日期键、有限样本修正与参考分布。
   旧规格表不能冒充对照。不能解释的差异只阻塞对应结果，不扩大审计。

【产出】
在results/Appendix/F/{tables,data,logs}/内以F02_前缀保存：
- 两个模型的完整逐系数表：模型/项名、系数、两种SE、two-way/LAD SE比、
  两种95%区间；既有完整表含p值时沿用其明确规则输出p值。
- 同样本来源与规格表，含样本数、设计秩、聚类数、修正及区间规则。
- 两种完整协方差矩阵及列名；保留已有函数提供的分量供复算，
  不为保存分量另行开发统计方法。
- 与原final two-way的对应及差异记录；实际复用/重建/拟合动作。
- 简短执行报告：点估计一致性、关键阵风项及既有交互项的SE变化，
  以及不确定性表述是否需要在后续成文时调整。
保存矩阵/残差等必要复算输入，或可独立重建它们的精确来源配置。
本任务以表格为主要结果，不强制增加图片。
不要将过程清单都当作论文必须排版的表；沿用统一登记区分用途。

【必要检查】
两种推断的样本/系数/列序一致；聚类键与行对应；协方差有限且对称；
SE平方与对应对角一致；区间计算符合记录规则；复用表与当前模型匹配。
如果出现负方差、非有限结果或现有函数进行数值修正，记录具体处理，
不靠abs、静默截零或删系数把异常藏起来，不新增一套纠偏实验。
SE比小于1或区间跨0是可报告结果，不是执行失败。
不预设“1.3–2.3倍”或其他历史范围，不与旧二次项的比值混作同量比较。
仅做本次有必要的检查，不重跑历史测试或统计实验。

【接口和覆盖】
接入main_appendix.py，支持单独F02调用，具体参数写README；
若现有选择机制足够就直接复用，不另建主入口。
普通运行可复用有效缓存，提供明确的本包重算方式；代码不依赖agent/LLM。
固定结果目录与稳定文件名，验证后覆盖本任务管理的旧输出。
保留F的其他结果、A–E及G–J、历史实验和人工文件；不增加时间版本目录。
更新F README、manifest、现有主张—需求—产物映射及F02缺口状态。
本地执行后记“产物已补齐，待反馈分析”，不能代替研究负责人关闭第二包。

【停止】
实际完成必要计算与结果输出，记录局部阻塞及可用范围。
不修改正文、不审核A–E写作、不重算F.3、不启动G/I或H。
不生成ZIP或回传包，不自动开展独立科学审查，不远程发布。
停在结果和简短事实报告，由研究负责人安排人工+agent审阅。
```

### 9.4 F02反馈时再决定

收到两模型逐项结果后，只讨论：同模型对照是否完成；SE变化对既有推断有什么影响；是否存在未解释的实质差异。完成后关闭F02，再设计G/I，不提前将后续工作加入此次执行。


## 10. F02反馈分析与关闭记录（2026-09-10）

### 10.1 证据与判断

来源：F02_EXECUTION_REPORT.md（本地记录2026-09-10 17:22:51 BST）。本对话全文阅读并复算六个重点项的SE比，均与报告一致；没有收到原始矩阵，不声称独立重建协方差。

**F02结果补全可以关闭，进入第三包G/I。**

必须纠正旧缺口叙述：原final two-way CSV已经有se_lad列。因此不是“LAD-only从未计算”，而是先前定位遗漏及完整配套结果尚未整理。本轮补齐完整协方差及分量、两套95%区间、来源及复算输入，新增OLS拟合为0；不得把本轮包装成首次发现两种SE差异。

| 事项 | 回传结果 | 处置 |
|---|---|---|
| 天气暴露final | n=9857，27列/秩27，LAD=105，日期=1001，交叉组=6122，df=104，结点11/24 | 符合已接受正式模型 |
| 天气恢复final | n=9254，29列/秩29，LAD=104，日期=995，交叉组=5870，df=103，单结点11 | 未误用全体恢复二次型 |
| 新旧系数 | 最大差0 | 主结果系数不变 |
| 新旧两类SE | 最大绝对差均低于6.2×10^-13 | 数值对齐；不需重算正文系数表 |
| 新旧既定t规则p值 | 最大差约1.3×10^-13 | 原推断数字未发现实质变化 |
| 计算规则 | 同X/残差/系数；LAD+date−交叉组；各分量有限样本修正 | 符合本包对照要求 |
| 暴露协方差 | 最小特征值−9.41676333×10^-6；无负对角；未修正 | 不阻塞单系数表，保留具体用途边界 |
| 恢复协方差 | 最小特征值1.1437388×10^-6；无负对角 | 未报告同类问题 |

### 10.2 如何解释结果

天气暴露三个已列阵风项two-way/LAD SE比约1.144、1.273、1.308；天气恢复三个已列项约2.061、2.336、1.683。恢复侧不确定性受聚类口径的影响更明显。六个重点项的95%区间是否包含0均未改变：恢复线性阵风项两种区间均含0，恢复hinge及阵风×降水项两种区间均不含0。

少数月份虚拟变量的区间是否跨0发生改变，记录即可，不触发重新选变量或重估模型。全部系数中有SE比小于1的项，不能表述为two-way必然增大每个标准误。

SE比的具体范围必须注明样本和项：不能沿用不加限定的“阵风项一律1.3–2.3倍”。这不否定采用two-way主推断，只需让措辞对应实际结果。

### 10.3 必须保存的写作修改记录

| 位置/用途 | 具体修改要求 | 证据来源 |
|---|---|---|
| F02缺口及执行历史 | “缺少LAD-only结果”改记为“原表已有se_lad；已完成配套区间、协方差和可复算整理” | F02报告范围与来源 |
| 方法§3.4及F.2 | p值与区间按每模型实际LAD数使用t(G_LAD−1)；天气暴露df104、恢复df103 | F02样本/聚类规则 |
| Table 5表头及注释（若仍为t110） | 改为依样本的自由度，或统一标t(G_LAD−1)并列实际值；不改已正确的p值 | 导师Extended原Table 5写p(t110)，F02数值已对齐本地实际规则 |
| §3.4的倍数说明 | 根据实际所指样本/项限定；天气重点项完整范围约1.14–2.34；不扩大成全部项一律变大 | F02重点项和完整表范围 |
| F.2技术记录 | 保留暴露矩阵非PSD、对角正及未修正事实，不将单系数对照等同于所有组合方差有效 | F02矩阵诊断 |

本轮只记录上述改写需求；不审核A–E写作、不直接修改Word。以上是当前明确证据引出的局部衔接，不是新一轮全稿审计。

### 10.4 非半正定问题的适当处置

两维聚类协方差估计并不保证在有限样本中半正定，这一现象本身不能用来断言代码出错；参考Cameron与Miller《A Practitioner’s Guide to Cluster-Robust Inference》§V.B：[作者公开稿](https://cameron.econ.ucdavis.edu/research/Cameron_Miller_JHR_2015_February.pdf)。

当前所有单系数方差为正，可按已记录近似推断规则生成本包单系数区间；这不意味着任意系数组合的估计方差也一定非负。不能单凭最小特征值的绝对量断言它是舍入误差。

第三包若实际导出该天气暴露模型的既有曲线置信带，应顺手保存实际绘图设计/对比向量a及a'Va（或该绘图函数真实采用的梯度方差）。如果这些实际值合法，继续导出；若出现实质负值，记录对应图和位置，保留点预测及其余合法输出，不用abs/静默截零掩盖、不自动正定化。只处理正在交付的图，不重新审计所有置信带或开展联合检验。该具体用图检查不改变F02单系数对照的关闭状态。

## 11. 第三步G/I设计与本地Codex指令

### 11.1 目标、主张和产物

本包把已接受最终模型的预测和残差变成可核实的图表数据，并补齐七场风暴的同规格描述结果。先确认实际图形身份，再复用/补算，不以图号猜算法。

| 部分 | 正文/附录作用 | 最小产物 |
|---|---|---|
| G.1 | 说明预测来自哪个模型、哪个样本、何种验证 | 模型—样本—预测来源表，含in-sample/OOF身份 |
| G.2 | 支撑最终模型预测—观测及现有校准图 | 逐行预测、现有分箱表、精简图形及总体误差 |
| G.3 | 支撑风响应形状侦察及拟合后分箱偏差 | 控制模型残差、最终残差分别保存；对应分箱/支持/已有区间和曲线数据 |
| I.1 | 七场风暴窗口及各模型可用样本 | 窗口定义、分组数量、训练范围/排除标记 |
| I.2–I.3 | 当前最终模型风暴期表现 | 逐事件预测，逐风暴及合并误差表，两结果分面散点图 |

导师Extended原Figure 3是剔除阵风项后控制模型的分箱残差；Figure 5含最终响应置信带与OOF校准；Figure 6含天气子样本响应与分箱残差。旧磁盘fig11/13/14与这些正文图号并非一一同号。必须按函数、图注和源模型建立映射。

### 11.2 预测类型必须分开

1. 控制模型残差：y减去不含阵风相关项的控制模型预测，回答“控制这些变量后观察到什么响应形状”。不是最终模型误差，也不是完整因果识别。
2. 最终全样本拟合及残差：用于现有形状/拟合诊断与描述性风暴散点，不称样本外验证。
3. LAD-OOF：仅由各训练折模型生成，供现有空间留出误差和校准；复用原折及预处理/结点选择规则。模型家族的nested-CV预测与最终固定结点的条件CV若不同，分开记录，不能只用一个“final”名称覆盖。
4. 开发期冻结模型预测后期：只有确实存在符合该训练范围的模型时才能如此命名。本包不强制增加该方案，不以某风暴位于确认期就自动认定预测为时间留出。

G优先覆盖现有图所用四组最终模型：全体暴露60437、全体正客户恢复51173、天气暴露9857、天气恢复9254；以已接受正式模型来源核实，不能删行凑数。全体暴露平台、全体恢复二次型、天气暴露11/24平台、天气恢复11单结点的身份分别保留。具体协变量和变换读取项目函数，不凭上述简称重写模型。

I主任务为全体暴露/恢复两个正式最终模型在既定七场窗口的描述。若已有相容OOF可直接切片，作为明确标记的补充；不为了增加风暴样本外证据重新做storm-only训练、七场组合留出或新时间实验。已有天气风暴结果仅在登记确实要求时整理，不自动扩成四组全面比较。

### 11.3 复用与补算边界

C已保存大量逐行预测和训练参数，F02已定位天气模型同样本设计/残差；这些是优先来源。只有汇总RMSE的历史组件不能冒充已存在逐行OOF。

允许本包必要的固定模型、无阵风控制模型，以及明确缺少逐行输出的既定五折流程重建。若原验证规定训练折内重选结点，则复用原搜索函数和冻结预算；不把全样本结点塞进折内后称nested CV，也不重跑C的88项比较、D的bootstrap/profile或新候选调优。原“仅导出、不重拟合”限制在本包必要范围内被这一授权替代。

比较旧/新函数时，只补正文或现有图明确需要的有限对照。若旧输出样本/控制项不同，先正确标记，不能将差异归因于函数改进。需要当前同样本对照才能支撑的现有图，允许用该既定旧规格作最小重建，记录控制差异；不自动改为另一项模型选择研究。

现有分箱边界、参考协变量、平移常数、区间方法和显示规则优先复用。不因不好看改变箱数。若源代码明确但只未导出数组，直接在该步骤保存数组；不从PNG反推数值。确实无分箱规则时，仅为必要诊断冻结一套简单规则并说明是本轮新增，不宣称复现旧图。

### 11.4 可直接发送的指令

```text
任务：APP-GI-PRED——补齐Appendix G的图形底层证据与Appendix I的最终模型风暴结果。

【状态与范围】
先阅读本台账第10–11节、AGENTS.md、main_appendix.py及G/I现有登记。
J03和F02已完成反馈分析并关闭结果补全，本轮只执行第三工作包G/I。
将F02关闭依据和写作待改项登记到既有台账，不重算F02或J03。
A–E已完成且由负责人审核文字；H留待第四步。本轮不改Word、不审核A–E写作。

【先建立准确映射】
1. 以已接受Extended生产稿及主张—附录索引为准，逐项定位G03、I02：
   正文位置/图注、实际图片路径、绘图函数、模型/样本、预测身份、数组来源。
   重点包括现有最终预测、校准、按阵风分箱残差，以及七场风暴散点。
   正文图号和磁盘fig11/13/14不是同一套编号，不能据名称认定它们都属于偏残差。
2. 输出简短覆盖清单：REUSE、EXPORT、DERIVE、REFIT_NEEDED、NOT_APPLICABLE，
   列原因和目标文件；记录后继续执行明确缺项，不停在清单或脚本骨架。
3. 定位C保存的预测/参数/实际折/预处理与F02天气设计和残差。
   只定位需要的模型，不扫描审计全部历史。样本数相同不等于模型兼容：
   需核对ID、响应、列序/变换、控制项、固定或训练选结点、折和训练范围。

【G：逐行预测及残差】
4. 覆盖现有图需要的四组正式模型，预期60437/51173/9857/9254。
   以正式管线核对实际ID，不改模型、原因类别或清洗规则。
5. 优先导出/派生已有兼容全样本预测与OOF。
   缺对象时允许重建相同固定OLS；缺折预测时允许复用既定五折训练流程补齐。
   保持原LAD分配、折内标准化及原结点选择规则，不加新验证切分/调优预算。
   全样本模型不能生成声称OOF的预测。模型家族nested-CV与固定结点CV分开标注。
6. 逐行保存event ID、样本组、日期、LAD、真实响应、预测、残差、
   预测尺度、模型ID、prediction_type、fold/训练来源和有效标记。
   主评分采用原拟合尺度：暴露log(1+C)，恢复log(T)，单位和log底读取正式定义。
   不把exp(预测)自动当作原尺度算术均值，不新增重变换校正实验。
7. 对形状侦察，明确无阵风控制模型到底删了哪些列。
   真正无阵风模型不应保留gust×pressure或gust×precip等阵风交互。
   若原代码与原文字不符，记录差异及实际含义；允许最小生成明确定义的控制残差，
   保留与旧图的对应，不静默把一种残差改名为另一种。
   控制残差、最终in-sample残差和最终OOF残差必须分别保存，不混画为同一种证据。
8. 复用原图分箱，保存边界/闭合规则、n、实际平均gust、平均观测/预测或残差，
   及原图采用的SE/CI和其计算方法。空箱NA，稀疏箱保留数量。
   控制残差的箱内区间与模型曲线two-way置信带不是同一统计对象。
9. 现有预测校准若为predicted-value ventiles，沿用规则，处理重复切点并记录。
   校准表保留每箱实际n；不为了贴近对角线重新选箱或删点。
   总体报告池化RMSE、MAE及偏差，统一偏差=预测−观测；
   R²如原图/表需要，写明分母和基准，不混用相关系数平方或训练均值基准技能分数。
   OOF逐行池化得分与对应既有汇总做一次对齐；差异先定位口径，不改数据追数字。
10. 对现有图明确需要的旧模型对照最大限度复用；若当前样本上必须补拟合，
    只补所需既定规格。不同样本/控制项的结果不能标为纯函数形式效果。
    不运行C全候选、D结点审计或新的模型选择。

【实际曲线数据与F02边界】
11. 导出已有响应曲线时保存gust网格、预测线、参考协变量、中心化/平移规则、
    既有区间及来源。均值响应置信带不能叫单事件预测区间。
    天气暴露F02 two-way矩阵有负特征值但对角为正：仅对本轮实际导出的该模型
    置信带保存真实设计/对比向量（或梯度）及对应方差。
    不用单系数SE独立相加代替完整协方差。
    若实际曲线方差非负，正常输出；若实质为负，明确受影响点和图，
    保留点预测及其他结果，不abs/静默截零、不自动正定化或开展新联合检验。
    这一检查不重开F02，也不强制新增本来不需要的曲线。

【I：七场固定风暴】
12. 复用Arwen、Dudley、Eunice、Franklin、Babet、Ciarán、Henk既定窗口。
    保存开始/结束、时区、端点规则和重叠处理；不要从风速/结果重新挑窗口。
    输出各窗口曝光样本、恢复清洗前后及必要排除数量。
    只在既有口径下汇总，不重开原始事件重构或原因字典审计。
13. 以正式全体暴露/恢复最终模型生成或提取窗口事件预测。
    首先复现当前主张需要的描述性散点；全样本拟合明确标in-sample descriptive。
    已有兼容OOF可直接切片并单列；不为了这一步新增风暴专用训练。
    后期风暴也可能参与全样本训练，不能只因处于确认期就标时间样本外。
14. 每风暴及七场去重合并输出n、RMSE、MAE、平均预测/观测、偏差；
    已有Pearson相关可作为描述量保留，无方差/小样本时返回NA并说明。
    不新做逐事件独立性假设的相关显著性检验。
    同一预测/样本口径比较，七场合并指标从逐行重算，不简单平均七个RMSE。
15. 恢复99分位截断沿用正式清洗来源、阈值与顺序，不在风暴内重估分位。
    明确哪些开放三角形代表超出训练范围的可计算正客户事件；
    占位符、非正时长和缺协变量是不同排除原因，不能全部画成尾部三角。
    如旧图保留可计算超阈事件的外推点，允许据正式模型生成显示，
    标display_only/excluded_from_primary_metrics；主评分仍限正式有效范围。
    不能把图外或训练范围外点静默纳入评分，也不因离群而删掉原本合法的评分点。
16. 不声称全样本恢复模型对未来事件作实时预测：客户数等输入沿用原任务定义。
    原稿“暴露追踪均值”“恢复相关显著”“清洗后完全消除短事件高估”等句子，
    只记录当前结果可支持程度；不为了维护原句去调模型或删数据。

【结果组织】
G固定输出results/Appendix/G/{figures,tables,data,logs}/，
I固定输出results/Appendix/I/{figures,tables,data,logs}/。
共享主预测优先在G/data保存一份，I按事件ID引用或保存必要风暴切片并注明来源。

G至少包括：图形—函数—数据映射；逐行预测/各类残差；完整分箱数据；
总体评分；实际需要的响应网格；精简预测/校准/残差图。
I至少包括：窗口/样本表；逐风暴与合并评分；风暴逐行数据和排除标记；
最终模型暴露/恢复分面散点。不要无目的复制同一正文图，多处用同一权威路径引用。

图表能由保存数组和固定配置重新生成。保留正式图PNG及项目已有的矢量格式，
不为格式另建大型绘图库。显示抽样或透明度处理不改变评分样本；若抽样则留绘图标记/种子。
核心结果表登记为论文候选，过程日志/组件清单留data/logs，不自动全部排入附录。

接入main_appendix.py，提供本包或G/I可选择执行，实际调用写README。
复用相容缓存；提供限定本包的重算选项；所有代码无需agent/LLM可独立运行。
稳定文件名，验证后覆盖本任务管理的旧输出，不生成时间版本目录。
保护其他附录、G/I人工文件及无关既有结果；更新根映射、manifest、G03/I02状态。
F02写作待改项纳入既有修订记录，正文此次不改。

【必要检查和停止】
检查：唯一ID/覆盖；响应尺度；训练/OOF身份；折内转换与选结点来源；
评分复算；分箱计数及图表数组一致；风暴窗口合并去重；恢复显示/评分分离。
每张最终图实际打开确认轴、图例和排除标记。只做相关检查，不跑全项目历史测试。
如果旧图数值/主张不能在当前规格下重现，给出实际结果和明确的文字待调整项，
不把不利结果当作缺口，不为关闭任务要求模型拟合得更好。
局部确实不可用则保留其他完整产物并指明影响，不用旧规格数据填位。

实际完成计算与结果输出后给简短GI_EXECUTION_REPORT：
完成/缺失清单、复用/派生/拟合数量、G核心误差与残差发现、I七场结果、
既有图/主张需调整的具体对应、异常及准确复现命令。
本地状态记“产物完成/待反馈分析”，不自动科学关闭本包。
不撰写独立科学审查、不修改Word、不生成ZIP、不远程提交、不开始H03。
由研究负责人安排人工+agent审核后再反馈。
```

### 11.5 第三包反馈与关闭标准

G03：当前需要的图已经有明确的预测/残差身份、逐行或网格来源和完整分箱数据；实际重现差异已说明，不依赖截图读数。

I02：七场窗口、最终样本、预测性质与排除规则清楚；当前模型的逐事件、逐风暴及合并结果可复算。

合并判定：没有尚未解释的实质数据/计算阻塞，能够据真实结果决定哪些句子保留或限定。预测较差、偏差未完全消失或某个旧措辞不成立都不是必须继续加实验的理由。若某张图有实际负方差等局部阻塞，只处理该图，不推翻已关闭包。

收到第三包结果并完成反馈分析后，再设计第四包H03。此处没有授权启动H。
