# 导师分析独立复现验收记录

验收日期：2026-09-09，Europe/London。

## 已完成的范围

`main_new.py` 已作为新的分析入口，顶部有 16 个步骤的 0/1 开关、运行目的、500/300 次 bootstrap 设置，以及复用已完成运行的参数。`analysis_new/` 保存学习后改编、补齐的自有程序，复用既有主回归、结果写入保护及自己的分析样本。生产分析不依赖导师的文件夹。

完整数值运行：`results/new/20260909164459/`，16:44:59 开始、16:55:32 完成全部 16 步。最终制表运行：`results/new/20260909165736/`。最终文档及全部结果所在目录：**`results/new/20260909170633/`**。后两次只启用制表/文档或文档，先验证旧运行的输入及所有产物 SHA-256，再复制、重新生成，验证了关闭建模步骤的增量用法。每次目的、开关、时间和依赖链在各目录 `run.json`。

最终目录有 83 个正式产物：37 个 CSV、20 个 JSON、2 份系数 XLSX（每份 12 张表）、17 张重新计算的分析图、1 张从自己的原稿提取的地图、4 份 DOCX、2 份计算摘要 MD。导师 `results/` 中的 58 个文件全部有本项目对应产物。

|内容|路径（相对 `results/new/20260909170633/results/`）|
|---|---|
|本次数值摘要|`COMPUTED_RESULTS.md`、`MODEL_SELECTION_REPORT.md`|
|12 模型比较、固定铰链探索、正式结点、bootstrap、ordinal/NB、事件脆弱性|`model_selection/`|
|最终回归和 121,656 行区域日面板|`final_models/`|
|天气归因子样本回归和验证|`weather_only/`|
|全部 17 张分析图及四份自动生成表格 JSON|`figures/`|
|R² 分解、时间分样、描述统计和论文草稿|`paper/`|
|探索报告|`Model_exploration_predicted_vs_observed.docx`|
|长稿、短稿和本项目重组计划|`paper/Extended_paper_draft.docx`、`paper/NatComms_extract_draft.docx`、`paper/Paper_reorganisation_plan.docx`|

## 数据与来源证据

自己的 E0 60,437 行、R0c 59,834 行 CSV 与导师 CSV 的 SHA-256 完全相同，指纹详见 `../NEW_ANALYSIS_GUIDE.md`。使用的输入始终是 `review_package/data/`，没有复制导师数据作为新输入。最终恢复时间回归按导师规则排除 8,661 条零客户数记录，保留 51,173 条；这是明确的分析子样本规则，不是两边数据不一致。

代码来源记录为 `source_inventory.json`，覆盖导师 11 个 Python 与 2 个 JS 文件及本项目对应实现。已有的 `review_package/code/run_main_regression.py` 直接复用；新的缺失衔接包括固定铰链搜索、最终结点写回、四份表格 JSON、文档路径、参考文献和地图输入。`pretestmain.py` 的写入保护被复用；旧脚本、数据快照和冻结研究包未被改写。

## 数值核对

逐文件机器核对见 `20260909165736_comparison.json`；随后 17:06 的运行仅修正文档分页，所有数值文件保持相同 SHA-256。最终完整性检查见 `20260909170633_integrity.json`。

核对使用数值 `rtol=1e-6, atol=1e-7`，没有把不一致文件默认算作通过：

|结果|文件数|说明|
|---|---:|---|
|数值/结构在严格容差内一致|36|包括两份 12 工作表 XLSX、模型比较、所有结点 bootstrap、两边际全体/天气平台、最终回归、天气回归、R² 分解和面板|
|PNG 覆盖和尺寸一致|17|图的数值来自独立计算，已在输出和文档检查中查看；不要求 PNG 字节相等|
|需要说明的差异|5|如下逐项解释，无文件缺失|

五个差异文件：

1. `model_selection/knots.json`：原有字段数值一致；本项目增加自由结点备份、最终选择说明和平台条件范围三个字段。
2. `model_selection/fragility_summary.json`：原有 ordinal、逐阈值 logit 和负二项结果一致；增加两个收敛标记，E0/R0c 均为 true。
3. `figures/pvo_rows.json`：导师 JSON 仍写 R0c 16 m/s、对应 RMSE 四舍五入为 1.2694，但其预测 CSV 已使用 17 m/s。本项目从当轮 CSV 生成，写 17 m/s、1.2693；预测 CSV 本身严格匹配。
4. `final_models/district_day_fragility.json`：相同面板上的 L-BFGS-B 优化末位差异；11 个参数超过严格阈值，最大 theta 差约 0.00015 m/s。插值数据、probit 和曲面系数一致。
5. `model_selection/fragility_lognormal.json`：近乎平坦的事件条件曲线导致 theta/beta 对优化末位敏感，21 个数值字段超过严格阈值。保留原始差异，没有截断、手工改值或复制导师参数。

进一步在 0.5–40 m/s 范围用 10,001 个点比较全部 26 条对数正态曲线，最大概率绝对差 **8.991991×10⁻⁶**，全部小于事先在核对程序中设定的 10⁻⁴。证据见 `20260909164459_curve_comparison.json`。这是曲线复现精度的证据，不是对模型科学适用性的背书。

## 关键结点和版本衔接

- 自由探索 E0 两结点为 14/26 m/s，500 次 LAD bootstrap 的 95% 区间分别为 13–15、24–29.525 m/s；R0c 因第一结点不稳定，保留单结点 17 m/s。
- 最终 E0 平台重新剖面估计为 14/25 m/s，300 次 bootstrap 区间为 13–15、24–28 m/s；天气子样本平台为 11/24 m/s。
- 导师当前绘图源码和部分保存结果混用了探索与最终结点。本项目把探索自由铰链与最终平台分开，避免最后一步覆盖前面结果的模型形式。
- 平台仅约束平均气压下的阵风主效应；交互项仍然存在。低风速斜率并未被约束为零。方法、公式、CV 范围和其他解释问题详见 `../NEW_ANALYSIS_GUIDE.md`。

## 验证记录

- 平台 Gram 优化逐候选与直接 QR 残差回归 SSE 比较，小数点后 8 位一致；测试通过。
- 原 pretest 的 11 项测试通过；新 Python 源码 AST 和 JS 语法检查通过。
- 完整 16 步从自己的样本跑通；两次关闭建模的增量制表/文档运行通过。
- 最终 83/83 个正式产物哈希、2 份输入哈希均通过；13 个科学计算模块与实际执行时的源码指纹一致。
- DOCX 经隐藏 Word 16 只读导出，Poppler 生成页面检查图。探索报告 16 页、长稿 15 页、短稿 8 页、重组计划 2 页，共 41 页；检查页面总览及关键表格/注释页的放大图，修复了表下注释分离、孤立标题、表格宽度和变量标签。最终后三份 DOCX 主文档 XML 与上一次已检查版本相同，探索报告的表下注释修正另行放大复查。QA 中间文件保留在本地运行 `qa/`，不纳入正式产物或 Git。

## 论文文字的范围

四份 Word 文件可独立生成，但其中长短稿和探索报告沿用导师的历史论述模板；回归、脆弱性、预测、描述统计和模型比较表已从实际计算生成，插图也为本项目计算。其他手填句子仍可能与更新后的模型形式、数字或论证范围不一致，首页已明确标注。这一轮完成的是代码学习、数值复现和文档生成，不代表已完成可投稿稿件的科学与文字校订。

模型比较表尤其不再照抄原稿手填 BIC：加回共同高斯常数后计算同样本 ΔBIC，明确 E0 平台额外加入温度平方、R0c 平台样本不同，以及哪些 CV 在折内选结点。未来论文修订应以 CSV/JSON 和本次计算摘要为依据。

## 逐步历史

`LOG.md` 已按 Step 1–7 记录学习、模块建立、测试、失败与修正、完整运行、增量运行、最终验收及相关路径。`results/new/20260909163143/` 是首轮开发失败记录；`20260909164707/` 是文档烟雾测试夹具，不作为完成的数值复现结果。
