
## 2026-09-05（工作命令 #44：C01专项修复——事件代表行按最早开始时间重新选取，核心模型重估对比 —— 已执行，生成回执。**注：这是独立编号的C01专项修复，不是常规工作命令编号延续，产出全部写入新目录`c01_repair_20260905/`，未覆盖任何历史文件**）

- 本机`.venv`直接执行，Step1-5全部完成，未分批次交付。确认bug位置：`v3_validation_pipeline.py`第64行`df.drop_duplicates(INCIDENT_COL)`默认`keep="first"`按文件行序（非按时间排序）选取代表行，该函数被本项目全部下游脚本间接调用。核实原始v3文件行序本身不按事件分组也不按时间排序（如事件FREP-231661-Z的13条阶段记录分散在source_row 1到237,716之间）。
- **Step1**：135,025个唯一事件中**9.54%（12,886个）代表行发生变化**，24,893个事件存在并列最早时间（已用source_row_number消歧）；变化事件的阵风差异均值0.25m/s、6,918个事件绝对差异超过1m/s。完整135,025行事件级对照表已产出，非仅汇总统计。
- **Step2**：复用`main1.py`原有`build_incident_features_from_hourly`函数（未重新实现），从75,983个本地天气缓存文件中按新代表行时刻重新索引，**95.21%（12,269个）成功从现有缓存恢复、无需新天气API调用**；617个（4.79%）无法恢复，已明确列出完整Incident Reference清单标记为"待新增天气请求"，未用旧数据顶替。
- **Step3**：主E0/主R0c样本代表行变化占比**13.57%/13.56%，与code_audit_20260905报告数字精确一致**，确认复现同一bug；开发/确认样本归属仅1个事件变化；**风暴归属变化数初次输出11,385个，经核查发现98.9%（11,261个）是`None`经pandas转`NaN`后`NaN!=NaN`恒真导致的比较伪影，已修正为真实变化124个**（其中59个是Eunice/Franklin背靠背风暴边界附近的合理跳转），如实报告并更正了诊断脚本自身的这一逻辑缺陷。
- **Step4**：通过猴子补丁`v9.step0_build_sample()`、其余下游代码逐行未改动的方式重建修复后样本（WT n=60,453/E0 n=60,437/R0c n=59,834，与修复前完全相同）。核心系数对比：E0二次项0.1028→0.1060（+3.1%）、R0c二次项0.0792→0.0850（+7.2%），**修复前基线额外用相同新方法重新计算**（命令#21本身未产出过最终样本5折稳定性表）以确保对比方法论一致。临界风速10.694→10.807 m/s（+1.06%），Delta/Bootstrap两种CI定性结论不变。customers/阵风权重比从12.7倍降至7.4倍（阵风边际贡献0.62pp→1.06pp）。5折CV%普遍改善（E0二次项15.83%→13.19%，R0c二次项23.74%→20.42%）。
- **Step5**：明确回答核心发现（二次项主导且稳健）完全成立，稳健性指标修复后不降反升；临界风速变化幅度小（+1.06%）两种CI检验均通过；customers解释力更大这一排序依然成立但权重差距收窄（12.7倍→7.4倍），已如实指出这一实质变化不应被笼统忽略。
- **产出文件**：`C01_repair_report.md`；脚本`step1_representative_row.py`、`step2_weather_reextraction.py`、`step3_4_corrected_sample_and_refit.py`、`step4b_fold_stability_and_variance.py`、`step4c_original_baseline_for_comparison.py`；完整原始数据见`raw/`目录（事件级对照表、天气重提取结果、修复前后系数表、5折稳定性表、方差分解表等）。
- 验收标准逐项确认：Step1给出精确到事件级别的完整对照表；Step2的617个不可恢复事件已明确列出完整清单；Step4模型重估确保只改输入数据不改模型设定（含额外补做的同方法修复前基线）；Step5判断完全基于Step4实际数字；如实报告并修正了诊断脚本自身的NaN比较缺陷（11,385→124），未回避自身失误也未简化真实存在的权重比变化。
