
## 2026-09-05（工作命令 #46：全部遗留待核实项统一核实——A02、Table 3/4.4 p值、Appendix F/G、C09 —— 已执行，生成回执。**注：这是C01→C02-C08→C09修复链路的收尾环节，独立于常规工作命令编号序列，产出全部写入新目录`c09_final_cleanup_20260905/`，未覆盖命令#45之前的既有产出文件本身**）

- 本机`.venv`直接执行，复用`corrected_sample_builder.py`一次性构建C01修正后组合样本，A02/Appendix G/C09-VIF三项分析合并在同一进程内完成以避免重复构建样本。
- **A02**：确认Figure 4/6命令#45版本确实仍是naive `np.exp(eta)`未做Duan(1983)修正；在C01修正数据上重新拟合得到smearing因子E0=10.4485（隐含低估90.43%）、R0c=2.1869（隐含低估54.27%），与命令#43修复前旧估计数值接近但为独立重新计算所得（非复用），已按命令要求给出选项(a)保留现状+图注声明与选项(b)应用修正+重新生成两张图的具体后果和实施成本，未代为选择。
- **Table 3/Section 4.4**：核实命令#45已产出的四个C01修正数据聚类系数表（LAD单聚类、LAD×date双向聚类，E0/R0c各一对），完整整理出Table 3所需的阵风一次/二次项单聚类p值，以及Section 4.4所需的阵风一次/二次项+阵风×气压交互项双向聚类p值，全部为本命令实际核实确认，非复用修复前p值。
- **Appendix F**：完整重新整理Table F1（E0，26个协变量）、Table F2（R0c，26个协变量）两张完整系数表，每行含系数+LAD聚类SE/p+双向聚类SE/p，非部分行patch。
- **Appendix G**：Table G1（weather_natural占比按阵风十分位，修正后6.93%–57.06%，与命令#26原始6.89%–56.10%相比判定"不构成实质性差异"）；临界风速(10.81m/s)以下事件占比对比（修正后65.03% vs 37.88%，较原63.70%/36.83%变化约1个百分点，已说明该变化主要由临界风速阈值本身从10.69→10.81m/s右移贡献，非单纯阵风值修正效应，定性结论不变）。
- **C09项目1**：确认命令#27 VIF检查（`step26_*.csv`）和LAD/date/双向聚类检查（`step28_*.csv`）均缺失配套生产脚本（`final_combined_analysis/`目录下只有`step30_counterfactual.py`一份存活），历史上以内联/临时代码方式执行，属真实可复现性缺口。
- **C09项目2**：确认原VIF检查故意不含年月固定效应哑变量（对齐v6论文Appendix Table B1口径，非无意疏漏）；本命令第一次尝试重新计算完整设计矩阵VIF时脚本本身有bug（遗漏常数项导致对数人口/剥夺率等非中心化协变量VIF虚假膨胀至42+），**已定位、修正并如实记录这一自身失误**；修正后确认完整设计矩阵（含全部年月FE）下最高VIF仅3.755/3.758（E0/R0c），"全部VIF<4"论文表述依然成立，且与C01修正前数字在小数点后3位精度一致。
- **C09项目3**：确认`v3_validation_pipeline.py`的`step0_build_sample`/`step1_lad_gapfill`/`step2_build_folds`均存在"看似只读实则写盘"的副作用；**发现并诚实披露本人自己编写的`corrected_sample_builder.py`只对`step0_build_sample`打了补丁，未同步覆盖后两者**，导致命令#44/45/46三次运行已实际覆盖`claude_branch/results/v3_validation/raw/step1_lad_gapfill.json`（原holdout n=23650→现23649）和`step2_folds.json`（原dev n=97663→现97664）两个历史文件，与既定"不覆盖历史产出"约束相抵触；已现场重新复现验证原始数值可完全追溯（非仅凭报告文字回忆），确认全项目`.md`报告均未引用过这两个具体文件名作为数字来源（故未造成论文可引用数字丢失），选择如实披露而非手工改写文件内容，并给出后续修复建议（扩展`_patch_v9()`覆盖范围）。
- **产出文件**：`C09_final_cleanup_report.md`；脚本`run_all_c09.py`、`c09_vif_fixed.py`（含诊断脚本`diag_vif_spike.py`、`diag_vif_original.py`留存于`scripts/c09_final_cleanup_20260905/`供追溯）；`raw/`目录下A02/Appendix G/VIF全部原始数据（含如实保留的VIF误算版本以资对照）。
- 验收标准逐项确认：A02两个选项均给出具体后果和成本未代为选择；Table 3/4.4 p值均为本命令核实确认的C01修正数据实际拟合结果；Appendix F两张表完整无缺行；Appendix G两项均给出"是否实质性差异"的明确判断；C09三项均如实报告，其中项目2、项目3均涉及本命令自身在核查过程中发现并诚实披露的失误（VIF计算bug、猴子补丁覆盖范围不足导致的历史文件覆盖），未因是"收尾清理"而降低核查标准或回避自身问题。
