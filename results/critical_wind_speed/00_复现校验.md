# Step 0：复现校验

调用命令#9脚本（`v3_validation_pipeline.py`）的确定性Step0-2函数，重建与命令#9完全相同的样本（121,313个天气匹配事件→117,298完备协变量事件）与5折`GroupKFold`（分组=incident_date_utc）切分，然后用相同规格重新拟合 `E0_prime`（log1p(customers_v2)）和 `R0c_prime_B`（log(duration_B)，控制customers_v2后）。

## 校验结果

| 模型 | 样本n | 与命令#9历史结果最大系数绝对差 | 通过 |
|---|---|---|---|
| E0' | 117,235 | 9.71×10⁻¹⁷ | ✅ |
| R0c'_B | 116,064 | 9.71×10⁻¹⁷ | ✅ |

两个模型逐折的 `z_gust_0h`/`z_gust_0h_sq`（及R0c'_B的customers项）系数与命令#9原始结果的差异均在浮点精度量级（~1e-17），**复现校验通过**，确认可以在此基础上继续Step1-6的拐点估计。
