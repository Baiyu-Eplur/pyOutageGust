# Step 2：构造新的交叉验证折

参照命令#5已验证过的方法，用 `sklearn.model_selection.GroupKFold`（分组变量=事件日期`incident_date_utc`，5折），对Step 0/1确定的121,313个事件（其中137个因LAD缺失会在后续建模时被剔除，折构造本身不受影响）构造全新的5折。折构造过程**不使用customers_v2/duration_A/duration_B任何信息**（outcome-blind），只用日期分组，与命令#5的原则一致。

## 强制自查结果

| 检查项 | 结果 |
|---|---|
| 参与折构造的事件数（日期有效） | 121,313 |
| 跨折的日期数 | **0**（通过） |
| 折0样本量 | 24,267 |
| 折1样本量 | 24,229 |
| 折2样本量 | 24,272 |
| 折3样本量 | 24,273 |
| 折4样本量 | 24,272 |

- 0个日期跨折，与命令要求的"日期不跨折"完全一致。
- 5折样本量高度均衡（24,229–24,273，极差仅0.18%），"折间样本量大致均衡"的自查通过。
- 未强加"站点/LAD不跨折"的额外约束（命令#5已确认这类约束不是原方法学要求，本次沿用同样的处理原则，只按日期分组）。

后续Step3-5的每个模型会在此折分配基础上，先应用各自的完备协变量/因变量筛选（如LAD21CD非空、customers_v2非空等），折标签本身保持不变。
