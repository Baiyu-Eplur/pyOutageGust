# Step 0：构建全量最终分析样本

将命令#16的干净开发样本（weather_natural+technical_asset，剔除locked_temporal_test后）与命令#19的locked_temporal_test确认样本（同样的Cause Code范围与构建流程）直接合并（concat），不做任何去重（按设计两者日期不重叠，理论上不应产生重复事件）。

## 验证结果

| 检查项 | 结果 |
|---|---|
| 开发样本量（命令#16） | 48,338 |
| Holdout样本量（命令#19） | 12,115 |
| 合并后样本量 | **60,453** |
| 求和校验（48,338+12,115=60,453） | ✅ 通过 |
| 日期重叠数 | **0** |
| Incident Reference重叠数 | **0** |
| 合并后唯一事件数 | 60,453 |
| 唯一事件数=行数校验 | ✅ 通过（无重复事件） |

**全部验证通过，合并操作干净、无重复、无日期冲突。**

## 一个有趣的交叉验证

合并后的样本量（60,453）与命令#12报告的、**去污染修复之前**的原始weather_natural+technical_asset完整样本量（同样是60,453）**完全一致**——这是一个很好的一致性佐证：命令#16的"剔除locked窗口"与命令#19的"仅保留locked窗口"，两者合并后精确重建出了去污染之前的完整总体，证明命令#16的日期切分操作没有遗漏或重复任何事件。

## E0/R0c可用样本量

| 模型 | 开发样本 | Holdout样本 | 合并后 |
|---|---|---|---|
| E0（customers_v2非缺失） | 48,323 | 12,114 | **60,437** |
| R0c（duration_B非缺失>0，customers_v2非缺失，各自p99截尾） | 47,840 | 11,992 | — |

## duration_B的p99截尾阈值：采用合并样本重新计算，而非简单相加两个子样本

**方法论说明**：命令#16和命令#19的R0c样本分别用了各自子样本计算出的p99阈值（开发样本194.80h、holdout样本167.37h）。对于"全量最终分析样本"，按本项目一贯做法（每个命令拟合时都在自己实际使用的样本上重新计算p99，例如命令#9-19全部如此），**在合并后的完整分布上重新计算p99阈值**，而不是沿用任一子样本的旧阈值或简单平均：

- 合并样本（duration_B非缺失>0）→ 重新计算p99 = **192.08小时**
- 应用此阈值后，R0c最终可用样本量 = **59,834**

这个新阈值（192.08h）落在开发样本阈值（194.80h）和holdout阈值（167.37h）之间，符合预期（合并样本的分布是两者的混合）。

## 后续步骤样本口径

Step1-6全部使用本步骤确立的：
- E0最终样本：n=60,437
- R0c最终样本：n=59,834（p99截尾192.08h重新计算）
