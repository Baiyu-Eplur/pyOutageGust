# Step 1：customers_v2剂量-反应曲线（阵风固定，只变customers_v2）

用命令#21 Step1在全量样本上拟合出的R0c最终模型（未重新拟合，直接复用同一个模型对象/规格），把阵风一次+二次项+阵风×气压交互项、全部非阵风气象协变量、5个区域协变量、年月固定效应，全部固定在与命令#21 Step3**完全相同**的参照值（每个非目标协变量取其在拟合设计矩阵中的实际样本均值，年月固定效应取各哑变量的平均发生率，即"平均日历"——与Step3处理阵风曲线时对customers_v2项的固定方式完全一致，未重新选择一套不同的固定值）。只让`log1p(customers_v2)`的z-score在观测分布第1到99百分位之间取50个网格点变化。

## 关键数字与一致性核验

| | 数值 |
|---|---|
| customers_v2观测范围（原始值） | 0 – 1,522.67 |
| 预测duration_B范围 | 1.37 – 8.20小时 |
| **最大/最小比值** | **5.9903** |
| 命令#21 Step5已报告比值 | 5.99 |
| 一致性核验 | **通过**，差异0.01%，视为同一数字（差异来源于此前口头报告时的小数位舍入） |

**核验通过，未发现需要排查的不一致，本命令的曲线数据与命令#21 Step5的结论完全吻合。**

## 曲线形状的一点说明（如实标注，非本命令核心任务但需要提及）

图中可见customers_v2从0附近开始，预测duration_B先陡峭上升到峰值（约customers_v2=30-40附近），再持续下降——这是一个先升后降的形态。**这与命令#17/#18已经深入排查过的"customers_v2-duration_B倒U型"现象是同一件事**：命令#18已经证实，这个倒U型主要是n_stages（阶段数）的构成效应导致，不是customers_v2独立于处理复杂度之外的因果非线性效应（控制n_stages后，一次项从正转负，倒U型的上升段基本消失）。本曲线直接使用的是R0c最终模型（未控制n_stages）的系数，因此曲线形状仍然呈现这个已知的构成效应导致的上升段，不代表一个新的、未被讨论过的发现——只是如实反映了当前"headline"模型规格（不含n_stages控制）下的样子。这不影响本命令的核心目的（比较customers_v2整体波动幅度与阵风的比较），因为最大/最小比值这个量化指标本身就是基于当前headline模型规格算出的，与命令#21 Step5的口径完全一致。

完整50个网格点数据见 `07_customers_v2剂量反应曲线.csv`。
